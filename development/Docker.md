# Mise en place de l'API via docker

Construire l'image docker associé à notre API :
> foo@docker$> docker-compose -f app.yml build

Lancer l'image docker : 
> foo@docker$> docker-compose -f app.yml up

# 